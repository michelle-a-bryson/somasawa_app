import 'package:flutter/material.dart';

class ObjectiveView extends StatelessWidget {
  const ObjectiveView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(16),
      child: Text('Objective Content'),
    );
  }
}
